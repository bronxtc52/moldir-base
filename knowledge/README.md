# Knowledge Folder

This folder is the source of truth for the project.

- Put untouched source materials in `raw`.
- Use `npm run index` to generate `entities` and `indices`.
- Put briefs and prompt templates in `templates`.
- Put future generated outputs in `generated`.

Do not delete raw files after importing them; they preserve provenance.
